# Livedata.Auth.CognitoMock is a real lib module (lib/livedata/auth/cognito_mock.ex).
# Tests use it via the :auth_provider config set in config/test.exs, routed through
# Livedata.Auth.Provider. This file is kept as a placeholder to document that the
# Mox-based mock was intentionally removed when the behaviour switched from OIDC to
# USER_PASSWORD_AUTH.
