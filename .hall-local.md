%hall-local v1 | MockaSort-Studio/a2t-mrv | 2026-05-29
!arch docs/design/data-model-measurement.md: measurement schema design doc — engine evaluation and schema decisions
!arch CLAUDE.md: repo project instructions (requirement traceability, @req annotations)
!arch CONTRIBUTING.md: contribution guidelines including @req annotation convention
!con append-only raw_measurements — no updates or deletes (CRCF-25)
!con dedup enforced at DB level via content_hash UNIQUE (CRCF-28) — must not be weakened
!con single-consumption of raw records enforced via UNIQUE(source_id) on derived_measurement_sources (CRCF-33)
!con stack is Elixir/Phoenix/Ecto; single-tenant
!con TimescaleDB rejected for measurement schema — UNIQUE(content_hash) cannot be globally enforced on hypertable; plain Postgres confirmed
!dec 2026-05-29 #17 tomashco: plain Postgres over TimescaleDB — hypertable partitioning incompatible with CRCF-28 global unique constraint on content_hash
!log 2026-05-29 #17 tomashco → pr_created: added docs/design/data-model-measurement.md with ## Database engine section; TimescaleDB evaluated and rejected
