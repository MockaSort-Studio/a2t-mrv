%hall-local v1 | MockaSort-Studio/a2t-mrv | 2026-06-17
!arch CLAUDE.md: lazy-import directive for contributing guidelines (@docs/contributing/*.md)
!arch CONTRIBUTING.md: pointer to docs/contributing/
!arch docs/contributing/general.md: ground rules, req traceability, source of truth refs
!arch docs/contributing/livedata.md: Phoenix/Elixir/Ecto/LiveView conventions for livedata/
!arch README.md: project overview and monorepo layout
!arch SETUP.md: dev environment setup (devenv, direnv, PostgreSQL)
!arch devenv.nix: dev env config — Elixir, Erlang, Node.js, PostgreSQL on port 5433
!arch livedata/mix.exs: Phoenix 1.8 + LiveView 1.1 + Ecto app definition
!con @req: annotation required on all requirement-covering code and tests
!con every new folder or package must include README.md and CONTRIBUTING.md
!con do not commit agent artefacts (.claude/, local CLAUDE.md overrides, cache/context files)
!con CLAUDE.md contains only common general rules and facts — no project-specific state or session data
!con new sub-technology introduced → create docs/contributing/ file + add @import in CLAUDE.md
!con source of truth: GitHub project board (dev discussion); Google Drive design doc (product vision)
!con run mix tasks from inside livedata/
!con PostgreSQL on Unix socket port 5433 via PGHOST; no TCP password
!con mix precommit before closing (compile --warnings-as-errors, deps.unlock --unused, format, test)
!con modules namespaced Livedata (domain) and LivedataWeb (web)
!con use :req (Req) for HTTP requests; :httpoison, :tesla, :httpc forbidden
!con never nest multiple modules in same file
!con changeset fields via Ecto.Changeset.get_field/2; no map access on structs
!con use mix ecto.gen.migration for all migrations; never hand-write timestamps
!con Ecto.Schema :string type for all text columns including :text
!con programmatic fields (e.g. user_id) must not appear in cast; set explicitly
!con HEEx templates only (~H / .html.heex); never ~E
!con forms via to_form/2 assign; never pass changesets to templates
!con LiveView streams for collections; never regular list assigns
!con no <.flash_group> outside layouts.ex
!con no inline script tags in templates; use colocated hooks (:type={Phoenix.LiveView.ColocatedHook})
!con colocated hook names must start with "." prefix
!con begin LiveView templates with <Layouts.app flash={@flash} ...>
!con use <.icon name="hero-..."> for icons; never Heroicons modules
!con use <.input> component for form inputs
!con LiveView class attrs must use list [...] syntax for multiple values
!con HEEx interpolation: {...} for attrs and values; <%= %> only for block constructs
!con no <% Enum.each %>; use <%= for item <- @collection do %> for iteration
!con bind block expression results (if/case/with); never discard return values
!con no String.to_atom/1 on user input
!con predicate functions: no is_ prefix; end with ?; reserve is_ for guards
!con tests: always use start_supervised!/1; avoid Process.sleep/1 and Process.alive?/1
!con tests: Process.monitor/1 + assert_receive :DOWN instead of sleeping to wait
!con no live_redirect/live_patch; use <.link navigate=/patch=> and push_navigate/push_patch
!con no phx-update="append" or "prepend"; use streams
!con no Phoenix.HTML.form_for/inputs_for; use Phoenix.Component.form/1 and inputs_for/1
!con no @apply in CSS; write Tailwind classes directly
!con no external vendor script src or link href in layouts; import via app.js/app.css
!con always add unique DOM IDs to key template elements (forms, buttons)
!con router scope blocks include alias prefix; no extra alias needed for route modules
!dec 2026-05-30 #16 tomashco: PostgreSQL + PostGIS confirmed as DB engine for spatial schema (recommendation accepted; not yet implemented in code)
